name: Continuous integrations GitHub,GitHub Actions,Docker Hub

on:
  push:
    branches: master 
  
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Docker Login
      uses: docker/login-action@v3.2.0
      with:
        username: ${{secrets.DOCKER_USERNAME}}
        password: ${{secrets.DOCKER_PASSWORD}}

    - name: Setup Java JDK
      uses: actions/setup-java@v4.2.1
      with:
    # The Java version to set up. Takes a whole or semver Java version. See examples of supported syntax in README file
         java-version: '21'
         distribution: 'adopt'   
         cache:  maven
    - name: Build Application Jar   
      working-directory: ./app
      run: mvn clean package
    - name: Docker compose Build   
      working-directory: ./ 
      run: docker compose build
              
              


Invalid workflow file: .github/workflows/continuous-integrations.yml#L17
The workflow is not valid. .github/workflows/continuous-integrations.yml (Line: 17, Col: 19): Unrecognized named-value: 'DOCKER_USERNAME'. Located at position 1 within expression: DOCKER_USERNAME .github/workflows/continuous-integrations.yml (Line: 18, Col: 19): Unrecognized named-value: 'DOCKER_PASSWORD'. Located at position 1 within expression: DOCKER_PASSWORD

 
